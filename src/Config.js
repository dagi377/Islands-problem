//
/**
 * cellSize : the size of the  square cell in pixel minimum is 2 pixels, just in case you want to see large array dimensions  n,m
 * density : the dencity of labeled bits hence defines how the islands are dense  optimal values are ( 0.6 to 0.9)
 */

const config = {
	"cellSize": 20,
	"density": 0.6
}
export default config;
